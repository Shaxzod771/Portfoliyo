# Copy this file to .env.local and fill in the values (.env.local is git-ignored).
# For GitHub Actions deploys, add the same names as repository secrets.

# Token from @BotFather
VITE_TG_BOT_TOKEN=
# Chat that receives contact-form messages (your user id, or a group id)
VITE_TG_CHAT_ID=
